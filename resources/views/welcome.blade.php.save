# 貼り付けて保存<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}"> <head> php artisan 
view:clear <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>nibiにようこそ！</title>
    @vite('resources/css/app.css')
<link rel="stylesheet" href="/build/override.css?v=1756708119">
</head>
<body class="antialiased bg-gray-100 dark:bg-gray-900">

    {{-- 右上の Log in / Register は従来どおり残す --}}
    @if (Route::has('login'))
        <div class="fixed top-0 right-0 px-6 py-4 text-right">
            @auth
                <a href="{{ url('/dashboard') }}" class="text-sm text-gray-700 dark:text-gray-300 underline">Dashboard</a>
            @else
                <a href="{{ route('login') }}" class="text-sm text-gray-700 dark:text-gray-300 underline">Log in</a>
                @if (Route::has('register'))
                    <a href="{{ route('register') }}" class="ml-4 text-sm text-gray-700 dark:text-gray-300 underline">Register</a>
                @endif
            @endauth
        </div>
    @endif

    <div class="min-h-screen flex items-center justify-center px-4">
        <div class="w-full max-w-3xl text-center">
            {{-- ここで Laravel のSVGロゴは完全削除し、アップしたロゴに差し替え --}}
            <img src="{{ asset('logo.svg') }}" alt="nibi logo" class="mx-auto h-32 w-auto sm:h-40 md:h-48">

            <h1 class="mt-8 text-2xl sm:text-3xl md:text-4xl font-bold text-gray-800 dark:text-gray-100">
                nibiにようこそ！
            </h1>
            <p class="mt-3 text-gray-600 dark:text-gray-400">
                ここから開発を始めましょう。
            </p>
        </div>
    </div>

</body>
</html>
